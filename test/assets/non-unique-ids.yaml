env:
  cache_root: /caches/%u

logging:
  log_level_console: Debug
  log_level_file: Debug

cache_mappings:
  - id: duplicated-id
    type: symbolic_link
    target: /tmp

  - id: duplicated-id
    type: bind_mount
    target: /tmp
